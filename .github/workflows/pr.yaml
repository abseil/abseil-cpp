name: trunk merge checks
on: pull_request

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: run a test
        run: |
          false
