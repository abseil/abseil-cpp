trigger: none

stages:
- stage: Bazel_Build
  displayName: Build
  jobs:
  - job: Build
    pool:
      name: 'VMSSPool'

    steps: 

    - script: |
        bazel version
      displayName: 'show bazel version'

    - script: |
        bazel clean 
      displayName: 'clean project'

    - script: |
        bazel build --remote_cache=http://remotecache.eastus.cloudapp.azure.com:9090 absl/...
      displayName: 'bazel build' 
